// Copy this file to `env.js` (gitignored) for local overrides.
// It lets index.html pick up local settings without editing the file.

// Option A: Set individual globals
// window.API_BASE = "http://localhost:8000";
// window.API_KEY = "mps-85-whampoa"; // or leave undefined

// Option B: Provide a single ENV object
// window.ENV = {
//   API_BASE: "http://localhost:8000",
//   API_KEY:  "mps-85-whampoa"
// };

