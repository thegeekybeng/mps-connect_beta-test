API_KEY: "mps-85-whampoa"
MODEL_DIR: "/app/api/artifacts_zs_hier_plus"
PROVIDERS_JSON: "/app/api/providers_map.json"
# Comma-separated list. Do not add spaces unless intended.
CORS_ORIGINS: "https://mps-connect-web-987575541268.asia-southeast1.run.app,http://localhost:3000,http://localhost:5173"
LOG_LEVEL: "INFO"
ENVIRONMENT: "production"

# Optional: external database URL (leave empty if not used)
DATABASE_URL: ""

# Government API keys (leave blank to use mock mode)
HDB_API_KEY: ""
MOM_API_KEY: ""
MSF_API_KEY: ""
IRAS_API_KEY: ""
TRAFFIC_POLICE_API_KEY: ""

# Security secrets (replace with secure values before production)
JWT_SECRET_KEY: "replace_me_with_secure_token"
ENCRYPTION_KEY: "replace_me_with_secure_token"
